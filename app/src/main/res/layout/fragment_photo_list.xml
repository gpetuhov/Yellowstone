<?xml version="1.0" encoding="utf-8"?>

<!--Layout for fragment with list of recent photos from Yellowstone-->

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--RecyclerView for list of photo-->
    <android.support.v7.widget.RecyclerView
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/photo_list_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!--This TextView is displayed when RecyclerView is empty
    (when network is not available)-->
    <TextView
        android:id="@+id/photo_list_empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:visibility="gone"
        android:text="@string/no_internet_connection"/>

</RelativeLayout>